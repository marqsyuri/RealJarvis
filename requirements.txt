# Jarvis Worker v2 — dependências
# pip install -r requirements.txt

# WebSocket e crypto (IoT Bridge)
websockets>=12.0
cryptography>=42.0

# Reconhecimento de voz
SpeechRecognition>=3.10
PyAudio>=0.2.14        # microfone (backend do SpeechRecognition)

# TTS — ElevenLabs streaming
httpx>=0.27.0          # streaming HTTP
sounddevice>=0.4.7     # playback PCM (sem dependência de player externo)
numpy>=1.26.0          # buffer PCM int16

# HTTP para brain_client
requests>=2.31.0

# Opcional: variáveis de ambiente em arquivo .env
# python-dotenv>=1.0.0
